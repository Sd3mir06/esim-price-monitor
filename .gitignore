# scratch / temp — never commit these
*.log
_*.py
__pycache__/
logs/

# keep: collect.py, build_dashboard.py, build_history.py, should_collect.py,
#       data/prices_*.csv/json, data/latest/*.json, docs/, .github/

# macOS
.DS_Store

# Kâr motoru — maliyet/marj verisi ASLA public olmamalı
local/
